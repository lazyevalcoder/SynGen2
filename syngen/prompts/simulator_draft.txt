You are a Business Intelligence engineer designing a synthetic dataset config. Given a story, acceptance criteria, and a spec, produce a simulator.json that can generate a dataset satisfying all criteria.

The generator engine supports EXACTLY this schema (do not invent fields):
HARD RULE on the quota block: scan the acceptance criteria FIRST. If none of them is a revenue_vs_plan check, you MUST NOT include a quota block at all - not even for phrases like "vs plan" or "below plan" without explicit per-segment plan numbers. Raking overrides deal-size knobs and will break other criteria.
{
  "seed": <int>,
  "time_model": {"fiscal_year": "FY26", "quarter_labels": [...], "quarter_end_dates": [...]},
  "output": {"workbook": "output/dataset.xlsx"},
  "accounts": {
    "count": <int>, "regions": {name: weight}, "segments": {name: weight}, "industries": [...],
    "territories": {"<territory>": ["<region>", ...], ...},
    "icp_share": <0-1>
  },
  "opportunities": {
    "per_quarter": <int>,
    "win_rate": <0-1>, "win_rate_jitter": <small fraction>,
    "owners": [names...],
    "deal_duration_days": [lo, hi],
    "volume_multipliers": [<per-quarter multiplier>],
    "close_clustering": {"share_in_end_of_quarter_window": <0-1>},
    "deal_size_lognormal": {"median_usd": <num>, "sigma": <num>},
    "discount": {
      "base_by_quarter": {"<region>": [<one value per quarter>], ...},
      "noise_sd_pp": <num>, "end_of_quarter_boost_pp": <num>,
      "end_of_quarter_window_days": <int>, "min_pct": 0, "max_pct": 40
    }
  },
  "products": {
    "catalog": [{"id": "<sku>", "tier": "<entry|core|premium>", "share": <0-1 or {"weights_by_quarter":[...]}>}],
    "margin_by_tier": {"entry": <0-1>, "core": <0-1>, "premium": <0-1>},
    "price_multiplier_by_tier": {"entry": <mult>, "core": 1.0, "premium": <mult>},
    "discount_delta_pp_by_tier": {"entry": 0, "core": 0, "premium": <-pp>},
    "cogs_inflation_by_quarter": [<one multiplier per quarter>]
  },
  "pipeline": {
    "stage_names": ["Discovery", "Validation", "Proposal", "Negotiation"],
    "share_open_by_quarter": [<one per quarter, 0-0.95>],
    "slippage_rate_by_quarter": [<one per quarter, 0-1>]
  },
  "quota": {
    "by_segment": {"<segment>": [<revenue target per quarter>], ...}
  },
  "capacity": {
    "by_territory": {
      "<territory>": {
        "headcount_plan": [<one per quarter>],
        "headcount_actual": [<one per quarter, optional>],
        "ramping_reps_by_quarter": [<one per quarter, optional>],
        "ramp_productivity_pct": <0-100, optional>
      }
    }
  },
  "ownership": {
    "unowned_share_by_quarter": [<one per quarter, 0-1>],
    "churn_share_by_quarter": [<one per quarter, 0-1>],
    "owner_pool": ["<name>", ...],
    "win_rate_multiplier_after_change": <0-1 for decline stories>
  },
  "activity": {
    "mean_touches_per_account_by_quarter": [<one per quarter>],
    "potential_tilt": <negative = activity aimed at LOW-potential accounts>
  },
  "forecast": {
    "commit_ratio_by_quarter": [<committed vs actual, one per quarter>],
    "commit_share_of_won_by_quarter": [<share of won deals flagged as commit>],
    "low_activity_bias": <0+; higher = commit biased toward zero-touch accounts>
  },
  "pricing_response": {
    "price_change_pct_by_quarter": [<one per quarter>],
    "elasticity": <negative>, 
    "potential_mitigation": <0-1>
  }
}

The optional quota block: include it whenever acceptance criteria use revenue_vs_plan, gap_concentration OR coverage_ratio checks, or the story gives explicit per-unit plan numbers ("Enterprise plan was $2M/quarter"). A vague phrase like "below plan" WITHOUT numbers or a matching criterion does NOT justify a quota block - omit it, because raking overrides deal-size knobs and would fight other criteria. When included, two parts:
- "by_segment" (or "by_territory" when criteria use dimension territory): the PLAN - what each unit was targeted to earn per quarter (authoritative numbers, sized near natural expected revenue). Exactly one of the two keys.
- "attainment_by_segment": OPTIONAL map of how actuals land RELATIVE to plan (default 1.0). The story defines these: Enterprise missed plan by ~5% -> "Enterprise": 0.95; Mid-Market beat by ~4% -> "Mid-Market": 1.04. The engine rakes deal sizes so closed-won revenue lands exactly at attainment x plan.
Size targets near naturally expected revenue: per_quarter * win_rate * median_usd * (1 - avg_discount/100), split across units by weight. revenue_vs_plan criteria need NO further calibration: raking makes attainment exact.

Products block: include it whenever any criterion uses product/tier/margin/price-point checks (blended_margin_trend, tier_share_shift, discount_margin_link, avg_price_by_tier) - those criteria fail structurally without it. Guidance:
- margin_by_tier is gross margin at list price (e.g., premium 0.75, core 0.6, entry 0.45); discounts reduce realized margin below it
- price_multiplier_by_tier scales deal size by tier (entry ~0.35-0.5 of core; premium ~2-3x); omit to keep all tiers at the base deal-size distribution
- discount_delta_pp_by_tier COUPLES discounting to tier margin (WS4): high-margin tier discounted harder -> {"premium": +4}; guardrail punishing premium discounting -> {"premium": -3}
- cogs_inflation_by_quarter multiplies every tier's cost each quarter ([1.0, 1.02, 1.04, 1.06] for steadily rising COGS)
- product shares may be {"weights_by_quarter":[...]} curves for mix-shift stories; per-quarter sums must stay ~1.0
- COUPLING CALCIULUS (use it): blended_margin%(q) ~= 100 - avg_cogs/(1 - won_avg_discount(q)/100). If criteria demand both a big discount increase and only a small margin decline, pick HIGH margins (margin_by_tier 0.7-0.85) so the discount rise erodes margin by the claimed amount, e.g. discounts 12->18 with cogs 0.25 gives margin drop of only ~2pp; with cogs 0.45 it drops ~5pp
- TIER SHARE CALCULUS: catalog 'share' values are DEAL-COUNT shares; tier_share_shift criteria measure REVENUE share:
  rev_share_tier ~= count_share_tier * price_mult_tier / sum_j(count_share_j * price_mult_j)
  Work backwards from the criterion's revenue-share targets before choosing share curves - e.g., for entry=20%->35% of REVENUE with multipliers entry=0.4/core=1/premium=2.5, entry COUNT share must go roughly 40% -> 60%

Calibration guidance from prior experiments - USE THIS RECIPE, do not re-derive it:
- won_avg_discount(q) = sum(region_weight * base[region][q]) + share_in_window * boost + sum_tier(rev_share_tier * discount_delta_pp_by_tier[tier]) - INCLUDE the tier-delta term when discount_delta_pp_by_tier is nonzero (e.g., +6pp premium delta on 30% revenue share adds +1.8pp to every quarterly average; lower base curves accordingly)
- realized/list pct(q) ~= 100 - won_avg_discount(q) + ~1.8pp revenue-weighting bonus
- When criteria PIN a level (avg_discount or realized_vs_list target) for a quarter, set base_by_quarter so blended won_avg_discount(q) = 100 - target_realized_vs_list(q) + 1.8 exactly at each pinned quarter (e.g., Q1 realized_vs_list 88% -> blended Q1 discount ~= 13.8). The loop should inherit correct levels, not discover them.
- win rate noise std at n deals/quarter = sqrt(p*(1-p)/n): pick per_quarter >= 300 so statistical criteria are reachable
- noise_sd_pp of 2.5-3.5 is a good default; it does not shift averages
- revenue_vs_plan criteria need NO calibration: raking makes attainment exact; just include a sensible quota block covering every segment the criteria name
- deal_duration_days may instead be {"means": [<per-quarter avg days>], "spread": <days>} for cycle-length stories (e.g., slowdown from ~45 to ~70 days -> means [45,53,62,70], spread 10)
- volume_multipliers (optional): per-quarter creation scaling, e.g. pipeline slowing ~20% by Q4 -> [1.0, 0.95, 0.88, 0.8]. Omit when volume is flat.
- deal_size_lognormal may add "medians_by_quarter": [<per-quarter median>] instead of median_usd for shrinking/growing deal-size stories
- accounts may add "icp_share": <0-1> - fraction of accounts flagged as ICP (drives the icp column on both sheets). Use for pipeline-quality stories.
- categorical weights (regions/segments/products) may be {"weights_by_quarter": [...]} for mix-shift stories; per-quarter sums across the dimension must stay ~1.0
- "outlier_deals": {"share": 0.01, "multiplier": 15} creates a few whale deals (scaled prices) for concentration stories
- territories: {"<name>": ["<region>", ...]} rolls regions up into sales territories; every referenced region must exist in regions. Use when criteria reference territories or gap_concentration with dimension territory (quota then uses by_territory keyed by TERRITORY names, not region names).

Pipeline block: include it ONLY when criteria use open-pipeline checks (stage_aging, slippage_trend, coverage_ratio, pipeline_concentration) or the story describes deals still in progress. Guidance:
- share_open_by_quarter is the fraction of each quarter's created opportunities that REMAIN OPEN at evaluation time; they get lifecycle stages and an expected_close_date instead of closing
- open deals do NOT count toward closed-won revenue - quota targets apply only to the (1-share) that closes, so size per_quarter UP accordingly
- slippage_rate_by_quarter pushes that fraction of open deals' expected close dates past their creation quarter's end (rising rates = "deals slipping" stories)
- stage_names are your lifecycle stages; keep them conventional unless the story names specific ones

Capacity block: include it when criteria use the effective_capacity check or the story discusses AE headcount, ramp, hiring, or capacity planning. Guidance:
- exactly one of "by_territory" / "by_region", keyed by existing territory (or region) names
- headcount_plan is what the annual plan assumed (fully productive heads); headcount_actual is who was really on staff - omit to make actual = plan
- ramping_reps_by_quarter counts reps still ramping each quarter; ramp_productivity_pct is a ramping rep's output vs steady state (e.g. 45)
- effective_capacity_pct is DERIVED: ((actual - ramping) + ramping * ramp_pct/100) / plan * 100. Story says "headcount at 98% of plan but effective capacity 85-90%" -> headcount_actual ~ 0.98*plan plus enough ramping reps at low productivity to drag the ratio into the band. Solve BEFORE drafting:
  eff% = ((A - R) + R*p/100) / P * 100

Ownership block: include it when criteria use unowned_account_share or post_change_revenue_decline, or the story discusses accounts changing hands or being left unowned. Guidance:
- unowned_share_by_quarter: share of accounts with NO owner that quarter (owner blank in the account_ownership sheet)
- churn_share_by_quarter: share of owned accounts reassigned a DIFFERENT owner vs the prior quarter
- win_rate_multiplier_after_change (e.g. 0.5): won-rate drag on accounts whose owner changed THIS quarter - use it for "expansion fell where AE ownership changed" stories (#9)

Activity block: include it when criteria use commit_no_engagement_share or the story discusses meetings/touches/engagement vs potential (#13). potential_tilt negative points activity at LOW-potential accounts ("activity up but aimed at the wrong accounts").

Forecast block: include it when criteria use forecast_vs_actual or commit_no_engagement_share, or the story quotes a forecast number vs actual ("forecast +9%"). commit_ratio_by_quarter IS the story's claim: 1.09 means committed ran 9% hot vs actual.

Pricing-response block: include it for price-change elasticity stories (#16): elasticity is per +1% price; potential_mitigation near 1 spares high-potential accounts so conversion damage concentrates in low-potential territories. Pair with the elasticity_differential check.

Quota by_motion: quota.by_motion {"New Logo": [...], "Expansion": [...]} plans by deal MOTION (an account's first deal = New Logo, later deals = Expansion). Use for expansion-vs-new-logo stories (#22); revenue_vs_plan works with dimension "motion". quota.attainment_ex_outliers {"<unit>": ratio} splits a unit's story into a HEADLINE attainment and an EX-WHALE core attainment (#25) - requires outlier_deals to be set; criteria then use revenue_vs_plan with exclude_outlier_deals true.

Worked example (for criteria: Q1 avg discount 10% +/-2, Q4 avg 16% +/-2, premium region APAC >= +3pp all year, regions AMER 0.45 / EMEA 0.30 / APAC 0.25, boost 6.5 with share 0.25):
- contribution of boost to every quarterly average = 0.25 * 6.5 = 1.6pp
- target blended Q1 base = 10 - 1.6 = 8.4 -> set AMER=EMEA=APAC Q1 base near 8
- target blended Q4 base = 16 - 1.6 = 14.4; APAC needs +3pp over others: others 13.5, APAC 17 -> blend = 0.75*13.5 + 0.25*17 = 14.375 ✓
- so base_by_quarter: AMER [8,10,12,13.5], EMEA [8,10,12,13.5], APAC [8,11,15,17]

Begin your reply with the opening brace { immediately - no preamble, no explanation.

Story:
{{story}}

Acceptance criteria:
{{criteria}}

Spec notes:
{{spec}}

Assumptions to use unless criteria say otherwise: fiscal year FY26 with EXACTLY these quarter boundaries (use verbatim): quarter_end_dates = ["2026-03-31", "2026-06-30", "2026-09-30", "2026-12-31"], quarter_labels = ["FY26-Q1", "FY26-Q2", "FY26-Q3", "FY26-Q4"]; regions AMER/EMEA/APAC; seed 42.

Respond with ONLY the JSON object for simulator.json.
