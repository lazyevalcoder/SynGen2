"""RevOps domain-pack plugin code: check library (single source of truth).

Declarative artifacts (manifest, claim matrix, prompt fragments) live in
repo-root packs/revops/; this package holds the Python implementations
the kernel binds through the pack contracts."""
